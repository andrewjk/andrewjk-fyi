{
  "name": "andrewjk-fyi",
  "version": "0.0.1",
  "description": "A Redraft site",
  "type": "module",
  "scripts": {
    "dev": "tb --dev",
    "build": "tsc --noEmit && tb --build",
    "preview": "tb --preview",
    "check": "tsc --noEmit",
    "db:list": "wrangler d1 migrations list DB --remote",
    "db:push": "wrangler d1 migrations apply DB --remote",
    "tail": "wrangler tail"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@redraft/adapter-cloudflare": "^0.0.11",
    "@redraft/site": "^0.0.15",
    "@torpor/adapter-cloudflare": "^0.0.26",
    "@torpor/build": "^0.1.24",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.19.1"
  }
}
