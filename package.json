{
  "name": "andrewjk-fyi",
  "version": "0.0.1",
  "description": "A Redraft site",
  "type": "module",
  "scripts": {
    "dev": "tb --dev",
    "build": "tsc --noEmit && tb --build",
    "preview": "tb --preview",
    "check": "tsc --noEmit",
    "db:list": "wrangler d1 migrations list DB --remote",
    "db:push": "wrangler d1 migrations apply DB --remote",
    "tail": "wrangler tail"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@redraft/adapter-cloudflare": "^0.1.4",
    "@redraft/site": "^0.2.2",
    "@torpor/adapter-cloudflare": "^0.2.3",
    "@torpor/build": "^0.4.11",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.49.0"
  }
}
