{
  "name": "andrewjk-fyi",
  "version": "0.0.1",
  "description": "A Redraft site",
  "type": "module",
  "scripts": {
    "dev": "tb --dev",
    "build": "tsc --noEmit && tb --build",
    "preview": "tb --preview",
    "check": "tsc --noEmit",
    "db:list": "wrangler d1 migrations list DB --remote",
    "db:push": "wrangler d1 migrations apply DB --remote",
    "tail": "wrangler tail"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@redraft/adapter-cloudflare": "^0.0.12",
    "@redraft/site": "^0.1.0",
    "@torpor/adapter-cloudflare": "^0.0.33",
    "@torpor/build": "^0.1.31",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.20.3"
  }
}
