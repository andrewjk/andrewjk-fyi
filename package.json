{
  "name": "andrewjk-fyi",
  "version": "0.0.1",
  "description": "A Redraft site",
  "type": "module",
  "scripts": {
    "dev": "tb --dev",
    "build": "tsc --noEmit && tb --build",
    "preview": "tb --preview",
    "check": "tsc --noEmit",
    "db:list": "wrangler d1 migrations list DB --remote",
    "db:push": "wrangler d1 migrations apply DB --remote",
    "tail": "wrangler tail"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@redraft/adapter-cloudflare": "^0.0.18",
    "@redraft/site": "^0.1.4",
    "@torpor/adapter-cloudflare": "^0.1.0",
    "@torpor/build": "^0.2.0",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.33.0"
  }
}
